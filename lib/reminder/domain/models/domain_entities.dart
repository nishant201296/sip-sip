enum PermissionResult {
  granted,
  requestAgain,
  showManualDialog,
  na;
}

const notificationId = "reminder_app";
const notificationChannel = "reminder_channel";
const notificationDescription =
    "Used for sending notification as a reminder to drink water";
